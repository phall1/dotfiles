---
description: Implement a scoped change with evidence and validation
---
Implement the requested change: $@

First inspect the relevant code and constraints. State the acceptance criteria, make the smallest coherent edits, follow existing patterns, and validate with the most targeted useful checks. Delegate independent reconnaissance or review when it materially improves confidence, while keeping one writer by default. Report changed files, commands and results, unresolved risks, and whether anything was staged.

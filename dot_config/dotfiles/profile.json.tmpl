{{ $services := true }}
{{ if hasKey . "services" }}{{ $services = .services }}{{ end }}
{{ dict "harnesses" (get . "harnesses" | default (list "opencode")) "services" $services "history" (get . "history" | default false) | toPrettyJson }}

# Cache and dependencies (not tracked)
cache/
node_modules/

# Lock files (auto-generated, cause stow conflicts)
bun.lock

# Config files ARE tracked in dotfiles
# (removed package.json, .gitignore from ignore list)
